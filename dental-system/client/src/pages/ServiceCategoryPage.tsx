import { PageShell } from '../components/PageShell'

export function ServiceCategoryPage() {
    return (
        <PageShell
            title="Danh mục dịch vụ"
            description="Nơi quản lý giá hiện hành của từng dịch vụ và lịch sử giá theo dạng timeline để phục vụ kiểm tra thay đổi."
            testId="page-services"
        />
    )
}